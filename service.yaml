apiVersion: v1
kind: Service
metadata:
  name: nginx-secure
  namespace: demo
spec:
  selector:
    app: afjhb1-nginx-demo
  ports:
  - port: 8080
    targetPort: 8080
  type: LoadBalancer
